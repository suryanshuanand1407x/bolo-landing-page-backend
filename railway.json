{
    "build": {
      "command": "pip install -r api/requirements.txt"
    },
    "deploy": {
      "rootDirectory": "api",
      "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT"
    }
  }